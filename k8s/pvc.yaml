apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: workout-db-pvc
  namespace: default
spec:
  storageClassName: ""
  volumeName: workout-db-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
